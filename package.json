{
    "name": "dashforge",
    "version": "1.0.0",

    "scripts": {
        "build": "parcel build index.html --no-source-maps --public-url ./ -d dist",
        "inline": "node scripts/inline.js",
        "build:single": "npm run build && npm run inline"
    },

    "devDependencies": {
        "inline-source-cli": "^2.0.0",
        "parcel": "^2.16.4"
    }
}
